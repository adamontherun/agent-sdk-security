# Minimal round-trip config used only to prove the Compose plumbing works
# (scaffold stage). Chapter 15 replaces this with a real allowlisting,
# TLS-inspecting configuration built up directive by directive.
http_port 3128
http_access allow all
coredump_dir /var/spool/squid
